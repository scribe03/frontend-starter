import { MouseDownUpDirective } from './mouse-down-up.directive';

describe('MouseDownUpDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseDownUpDirective();
    expect(directive).toBeTruthy();
  });
});
